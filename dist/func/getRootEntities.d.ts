import { SyndicateRoot, SyndicateEntities } from '../types';
/**
 * Returns an array with all the children of the root entity
 *
 */
export declare function getRootEntities(root: SyndicateRoot): SyndicateEntities;
