import { SyndicateRoot } from '../types';
/**
 * Returns a rendered string with a visualisation of the structure
 *
 */
export declare function renderListStructure(root: SyndicateRoot, includeNull?: boolean): string;
